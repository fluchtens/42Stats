import { initDatabase } from "@/tasks/updateDatabase";

export default async function Home() {
  // await initDatabase();

  return (
    <main className="">
      <h1>42stats</h1>
    </main>
  );
}
